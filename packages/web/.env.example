# Supabase configuration (server-only — not exposed to the browser)
SUPABASE_URL=your_supabase_url
SUPABASE_PUBLISHABLE_KEY=your_supabase_publishable_key

# API server URL (MCP server with admin routes, default port 3001)
API_URL=http://localhost:3001
